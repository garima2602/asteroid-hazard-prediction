# Copy to .env.production (or .env) and set to your deployed backend's URL.
# Leave unset for local dev — Vite's proxy handles /api automatically.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
